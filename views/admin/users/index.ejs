
<div class="p-4 bg-white">
    <div class="overflow-x-auto">
        <table id="myTable" class="table table-xs">
            <thead>
                <tr>

                    <th class="text-black">Name</th>
                    <th class="text-black">Email</th>
                    <th class="text-black">Role</th>
                    <th class="text-black">Actions</th>

                </tr>
            </thead>
            <tbody>
                <% users.forEach(user=> { %>
                    <tr>
                        <td class="text-xs font-light">
                            <%= user.name %>
                        </td>

                        <td class="text-xs font-light">
                            <%= user.email %>
                        </td>

                        <td class="text-xs font-light">
                            <%= user.role %>
                        </td>








                        <td class="flex justify-center items-center">
                            <a href="/places/edit/place/<%= user._id %> "
                                class="btn btn-success btn-xs mx-1">
                                <i class="fa-solid fa-pen text-white"></i>
                            </a>
                            <a onclick="return confirm('Are you sure you want to delete this item?');"
                                href="/users/delete/user/<%= user._id %> "
                                class="btn btn-error btn-xs mx-1">
                                <i class="fa-solid fa-trash text-white"></i>
                            </a>
                        </td>

                    </tr>
                    <% }) %>

            </tbody>

        </table>
    </div>
</div>