// cashe data
let cachedData = null;

// Function to load data from MongoDB
const loadData = async () => {
    if (cachedData) {
        
        return cachedData;
    } else {
        
    }
};


export default loadData;